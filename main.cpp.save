#include <iostream>
#include <ostream>
#include <algorithm>
#include <iterator>

#include "LapinManager.h"
#include <cmath>

using namespace std;

int main(int, char **) {
    unsigned years = 5, repl = 10;
    unsigned res[repl];// = new unsigned[repl];
    LapinManager laps;

    for(unsigned i = 0; i < repl; i++) {
        res[i] = laps.simulation(years * 12, false);
        laps.reset();
    }

    std::copy(res.begin(), res.end(), std::ostream_iterator<unsigned>(std::cout, " - "));

    //delete [] res;

	return 0;
}

